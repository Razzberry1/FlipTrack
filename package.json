{
  "name": "flip-tracker",
  "version": "1.0.0",
  "description": "Flip tracking application for reselling business",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\" \"npm run bot\"",
    "start:no-bot": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd server && npm start",
    "client": "cd client && npm start",
    "bot": "cd bot && npm start",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install && cd ../bot && npm install",
    "build": "cd client && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
